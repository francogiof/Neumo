// Placeholder component kept for compatibility.
// This project no longer exposes any pricing UI. The file exists solely
// to avoid accidental import errors from other modules.

export function AirPlaceholder(): null {
	// Intentionally returns null — replace with real components for AQ previews.
	return null;
}

export default AirPlaceholder;
